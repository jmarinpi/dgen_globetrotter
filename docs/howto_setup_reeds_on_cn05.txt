####################################################################################
# NOTE: This document in progress and should NOT be used until it has been completed and fully tested
####################################################################################

# create a gams folder
mkdir gams
# move into folder
cd gams
# download gams installer
wget http://d37drm4t2jghv5.cloudfront.net/distributions/24.4.1/linux/linux_x64_64_sfx.exe
# make the installer executable
chmod 0755 linux_x64_64_sfx.exe 
# install gams
./linux_x64_64_sfx.exe
# move into directory
cd gams24.4_linux_x64_64_sfx/
# add GAMS to PATH variable
PATH=$PATH:/home/[username]/gams/gams24.4_linux_x64_64_sfx/
export PATH
# add license file
echo '[copy paste your licensing info here]' > gamslice.txt 
# or you can use winscp to put the file in /home/[username]/gams/gams24.4_linux_x64_64_sfx/
# set up default solvers
./gamsinst
	# walk through prompts and select default solvers
	# at the end
 you will be prompted to select option for read and execute permissions. 
	# choose option 1. useronly
# confirm that gams is installed correctly
# move to home folder
cd ~
# try running gams
gams
# make sure license file is valid
etc.

# set up REEDS
# move into github folder
cd ~/github
# clone REEDS repo from github
git clone git@github.nrel.gov:ReEDS/ReEDS.git
# move to downloads folder folder
cd ~/tmp
# download GDXRRW binary for linux (this is an R library that links R and GAMS)
wget http://support.gams.com/lib/exe/fetch.php?media=gdxrrw:gdxrrw_0.4.0.tar.gz -O gdxrrw.tar.gz

# launch R
R
# install R dependencies
install.packages('ggplot2')
# select a CRAN Mirror: type '87' then ENTER
install.packages('mapproj')
install.packages('reshape2')
install.packages('plyr')
install.packages('knitr')
install.packages('markdown')
install.packages('xtable')
install.packages('gdata')
install.packages('Hmisc')
install.packages("~/downloads/gdxrrw.tar.gz", repos = NULL)
# quit R
quit()
# do not save workspace
n


