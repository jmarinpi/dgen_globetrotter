0. Put/Move everything in the folder under the same Directory as LOAD_DATA
   Make sure any file paths within the following two R.code files are correct;
   Also make sure you have installed all R packages.

1. Copy MODEL_RUN_PARAMETERS_woDR2.dat under ./LOAD_DATA/

2. Run calc_input_to_GAMS_noDR_CG2.R

3. Run read_GAMS_RUNS3_CG2.R
